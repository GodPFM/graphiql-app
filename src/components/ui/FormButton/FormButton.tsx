import { styled } from '@mui/material/styles';
import { Button } from '@mui/material';

export const FormButton = styled(Button)(() => ({
  textTransform: 'none',
  color: '--tw-color-border',
  fontFamily: 'Source Sans Pro',

  // borderRadius: '10px',
}));
